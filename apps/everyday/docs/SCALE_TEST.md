# Масштабный тест mesh

## Что проверяется

`npm run scale:test` запускает 100 настоящих экземпляров production Rust-бинарника.
У каждого процесса собственные TCP-порт, SQLite-база, node ID и два worker-потока.
Это не симуляция записей внутри одного процесса.

Топология разреженная: кольцо плюс резервные связи с шагом 10. Каждый узел имеет
не более четырёх прямых соседей. Такая схема сохраняет связность при части
отказов и не создаёт O(N²) соединений.

Сценарий проверяет:

1. запуск и health всех процессов;
2. распространение организации и аккаунта с root на 100/100 баз;
3. офлайн-вход на удалённом узле;
4. подписанное сообщение и выдачу оборудования на удалённом узле;
5. доставку событий к root с сохранением Ledger V2 и device-proof;
6. работу после одновременной остановки 10 процессов;
7. перезапуск этих процессов на прежних базах и догон журнала;
8. догрузку и SHA-256-проверку CAS-вложения после восстановления;
9. отсутствие дублей, соблюдение лимита peers, RSS и объём обмена.

Отдельный `npm run mesh:test` разрывает все соединения одного узла, выполняет
на нём подписанные выдачу инструмента и отправку сообщения, дожидается
нескольких неудачных фоновых попыток, а затем возвращает соседей с прежними
базами. Операции автоматически доставляются без ручного `pullNow` и появляются
на каждой ноде ровно один раз. Это проверяет устойчивую отложенную доставку
store-and-forward, а не только кратковременный сетевой сбой.

## Контрольный результат 2026-08-31

| Метрика | Результат |
| --- | ---: |
| Узлы | 100 |
| Первичная сходимость | 16,5 с |
| Подписанная операция leaf → root | 2,0 с |
| Одновременно отключено | 10 узлов |
| Восстановилось и догнало журнал | 10/10 |
| RSS всего | 767,8 МБ |
| RSS p95 на процесс | 8,6 МБ |
| Отправлено | 4,73 МБ |
| Получено | 4,72 МБ |
| Максимум прямых peers | 4 |

Повторный прогон после внедрения account-frontier (`meshkeeper-sync/3`):

| Метрика | Результат |
| --- | ---: |
| Узлов | 100 |
| Первичная сходимость | 19,7 с |
| Leaf → root | 2,0 с |
| Восстановились после отказа | 10/10 |
| Учтённый sync-трафик | 8,84 МиБ |
| RSS p95 | 8,0 МиБ |

На коротком тестовом журнале трафик уменьшился примерно на 6,5%. Отдельный
`sync:test` создаёт длинную историю из 80 последовательных изменений и требует,
чтобы steady-state delta без новых событий занимала менее 35% полного snapshot.
При неизвестной или расходящейся голове протокол намеренно возвращает полную
конкретную account-chain: экономия трафика не имеет приоритета над восстановлением.

Среда контрольного запуска: Linux-контейнер, 20 ГБ RAM, loopback transport,
release-бинарник, отдельная SQLite-база на узел.

Контрольный прогон после автономного QR-onboarding и проверки восстановления CAS:

| Метрика | Результат |
| --- | ---: |
| Узлов | 100 |
| Запуск и bootstrap | 55,6 с |
| Первичная сходимость | 19,4 с |
| Leaf → root | 6,1 с |
| Восстановились после отказа | 10/10 |
| Восстановили CAS-вложение | 10/10 |
| RSS всего | 991,4 МиБ |
| RSS p95 | 10,3 МиБ |
| Отправлено | 7,57 МиБ |
| Получено | 16,61 МиБ |
| Максимум прямых peers | 4 |

Повторный прогон 2026-09-01 после capability-изоляции и Android Keystore/JNI:

| Метрика | Результат |
| --- | ---: |
| Узлов | 100 |
| Запуск и bootstrap | 49,0 с |
| Первичная сходимость | 17,5 с |
| Leaf → root | 8,1 с |
| Восстановились после отказа | 10/10 |
| Восстановили CAS-вложение | 10/10 |
| RSS всего | 923,3 МиБ |
| RSS p95 | 9,7 МиБ |
| Отправлено | 6,91 МиБ |
| Получено | 15,94 МиБ |
| Максимум прямых peers | 4 |

## Как повторить

```bash
npm run build:server
npm run scale:test
```

Для быстрого диагностического запуска количество можно уменьшить, но такой
прогон не является доказательством требования 100 узлов:

```bash
MESHKEEPER_SCALE_NODES=10 npm run scale:test
```

## Границы результата

История операций передаётся инкрементально по frontier каждой подписанной
account-chain; старые peers обслуживаются полным snapshot. Справочники, текущее
состояние и сообщения пока остаются в snapshot целиком. Поэтому тест доказывает
работу 100 узлов и отсутствие повторной пересылки длинной ledger-истории, но не
доказывает приемлемый трафик для миллионов файлов или тысяч операций в минуту.
Для следующего масштаба потребуется entity-frontier и нагрузочный профиль
существующего chunked content-addressed транспорта. BLE, Wi‑Fi Direct и LoRa
также требуют собственных тестов пропускной способности и фрагментации пакетов.
